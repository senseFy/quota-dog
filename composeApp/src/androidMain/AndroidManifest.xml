<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:label="QuotaDog">
        <meta-data
            android:name="saien.quotadog.build.COMMIT"
            android:value="${quotadogBuildCommit}" />
        <meta-data
            android:name="saien.quotadog.build.COMMIT_SHORT"
            android:value="${quotadogBuildCommitShort}" />
        <meta-data
            android:name="saien.quotadog.build.DIRTY"
            android:value="${quotadogBuildDirty}" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
